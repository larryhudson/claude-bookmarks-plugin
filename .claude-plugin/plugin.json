{
  "name": "bookmarks",
  "description": "Drop bookmarks into the session transcript without consuming assistant context. Prefix a message with 'bm:' to bookmark it.",
  "version": "0.6.1",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/bookmark.sh"
          }
        ]
      }
    ]
  }
}
